ATLAS_DB_NAME=atlas
ATLAS_DB_USER=atlas
ATLAS_DB_PASSWORD=atlas-local
ATLAS_DB_PORT=55432
ATLAS_DB_URL=jdbc:postgresql://localhost:55432/atlas

# The first vertical slice uses a deterministic fake. Do not add a live model
# key until the model adapter milestone introduces explicit cost and privacy gates.
ATLAS_AI_PROVIDER=fake
